generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Sucursal{
  id            Int   @id @default(autoincrement())
  telefono      String
  direccion     String
  codigoPostal  String
  horarios      String
}